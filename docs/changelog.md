# سجل تطوير مشروع NovaFlow ERP 🚀

## [1.6.1] - إصلاح نظام إنفاذ الصلاحيات (Action Guarding Fix)
### ✅ التحديثات التقنية التفصيلية (Technical Updates)
- **إصلاح تمرير الصلاحيات (Permission Propagation)**: تم حل مشكلة `UNAUTHORIZED_ACTION` عبر التأكد من تمرير مصفوفة صلاحيات المستخدم الحالي (`permissions`) لكافة الـ Services عند إنشائها (مثل `RoleService` و `LeaveService`).
- **تحديث الـ Hooks**: ربط الـ Services بـ `usePermissions` لضمان الحصول على أحدث حالة لصلاحيات المستخدم من الـ Context.
- **تحسين رسائل الرفض**: إظهار رسائل تنبيهية (Toasts) واضحة ومترجمة عند فشل تنفيذ إجراء بسبب نقص الصلاحيات.

## [1.6.0] - نظام الصلاحيات الفعلي (Phase 3: Action Enforcement)
### ✅ التحديثات التقنية التفصيلية (Technical Updates)
- **إنفاذ الصلاحيات (Action Guarding)**: تطوير كافة الـ Services (Project, Leave, Role, etc) لتشمل طبقة "إنفاذ" (Enforcement) ترفض تنفيذ العمليات البرمجية إذا لم يمتلك المستخدم الصلاحية المطلوبة في مصفوفته، حتى لو تم استدعاء الدالة يدوياً.
- **إدارة المشاريع**: حماية `createProject` بصلاحية `projects:create` و `completeStage` بصلاحية `projects:edit`.
- **الموارد البشرية**: حماية تقديم الإجازات بصلاحية `hr:create` واعتمادها بصلاحية `hr:edit`.
- **الأدوار الحساسة**: قصر عمليات تعديل أو حذف الأدوار الوظيفية على المطورين أو من يملكون صلاحية النجمة (`*`) فقط.
- **نظام تنبيهات الصلاحيات**: تحديث الواجهة لإظهار رسائل رفض واضحة (Toasts) عند محاولة تنفيذ إجراء غير مصرح به برمجياً.

## [1.5.1] - إصلاح استقرار مصفوفة الصلاحيات
- **إصلاح أخطاء الواجهة**: معالجة خطأ `Card is not defined` في مصفوفة الصلاحيات.
- **توثيق الإصدار**: تحديث اللوج ليعكس الإصلاحات التجميلية والبرمجية.

## [1.5.0] - مصفوفة الصلاحيات المتقدمة (Permissions Matrix)
- **تصميم استراتيجي**: تحويل إدارة الصلاحيات إلى مصفوفة (Module/Action) تتيح التحكم الدقيق (عرض، إضافة، تعديل، حذف، ميزات متقدمة).
- **تجربة مستخدم محسنة**: تقسيم الشاشة لسهولة التنقل بين الأدوار مع زر "تفعيل الكل" الذكي.

... (المحتوى السابق) ...
