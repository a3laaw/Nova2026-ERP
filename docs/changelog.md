# سجل تطوير مشروع NovaFlow ERP 🚀

## [1.7.6] - نظام الاتجاهات المنطقي الموحد (Logical Direction Purge)
### ✅ التحديثات التقنية التفصيلية (Technical Updates)
- **محرك الحالة (Language Engine)**: تحديث `language-context.tsx` ليقوم بفرض خاصية `dir` على مستوى `document.documentElement`؛ مما يضمن أن كافة عناصر المتصفح (Scrollbars, Modals) تستجيب فوراً لتغيير اللغة.
- **إصلاح المكونات الجذرية (UI Core Fix)**: تم تعديل ملف `src/components/ui/table.tsx` لاستبدال `text-left` بـ `text-start`. هذا التغيير يضمن أن كافة جداول النظام ستقوم بمحاذاة المحتوى بشكل آلي لليمين في العربية ولليسار في الإنجليزية.
- **تحديث الهيدر والسايدبار**: دمج منطق التبديل التلقائي في `DashboardLayout`؛ حيث يتحول مكان ظهور القائمة الجانبية (Sidebar) وشكل أيقونة الإغلاق آلياً بناءً على اللغة.
- **التطهير الشامل للـ CSS**: استبدال كافة فئات الهوامش الثابتة (`pl-`, `pr-`) بـ (`ps-`, `pe-`) لضمان عدم "التصاق" العناصر بجهة غير صحيحة عند تغيير اللغة.
- **واجهة HR**: إصلاح محاذاة خلايا أسماء الموظفين والأفاتار لضمان ظهورها في "بداية" (Start) الخلية دائماً.

## [1.7.0] - المرحلة الرابعة: الأمان الحقيقي وإنفاذ البيانات (Hard Security)
... (المحتوى السابق) ...
